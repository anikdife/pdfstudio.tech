import type { PdfDocModel } from '../state/types';

// Phase 1 stub.
// TODO(Phase 2): draw overlays onto PDF pages (pdf-lib).
export async function stampOverlays(_pdf: unknown, _doc: PdfDocModel): Promise<void> {
  return;
}
